# Terraform
infra/**/.terraform/
infra/**/.terraform.lock.hcl
infra/**/terraform.tfvars
infra/**/*.tfplan
infra/**/tfplan

# GCP credentials — never commit these
terraform-key.json

# Node (matches anywhere in tree)
node_modules/
dist/
dist-server/

# Env
.env
.env.local
